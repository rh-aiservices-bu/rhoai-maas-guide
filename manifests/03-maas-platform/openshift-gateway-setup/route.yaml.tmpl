apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: maas-default-gateway-https
  namespace: openshift-ingress
spec:
  host: ${MAAS_HOSTNAME}
  port:
    targetPort: https
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: Redirect
  to:
    kind: Service
    name: maas-default-gateway-openshift-default
    weight: 100
