apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - maas-gateway-options-cm.yaml

patches:
  - path: gateway-patch.yaml
    target:
      kind: Gateway
      name: maas-default-gateway
      namespace: openshift-ingress
